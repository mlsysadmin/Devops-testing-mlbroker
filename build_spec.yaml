version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash      
steps:
  - type: Command
    timeoutInSeconds: 1200
    name: "Build container image"
    command: |
#      cd ${OCI_WORKSPACE_DIR}/node_app
      docker build -t frontend  .
outputArtifacts:
  - name: myapp-image
    type: DOCKER_IMAGE
    location: frontend:v1.0
